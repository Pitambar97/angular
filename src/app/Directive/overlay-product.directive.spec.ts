import { OverlayProductDirective } from './overlay-product.directive';

describe('OverlayProductDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayProductDirective();
    expect(directive).toBeTruthy();
  });
});
